<h1>Edit Post</h1>

  <%= form_for @post do |f| %>
    <%= f.text_field :title, size: "60x1" %>
  <div>
    <%= f.text_area :body, size: "60x12" %>
  </div>
  <div>
    <%= f.submit "Update" %>
  </div>
    <% end %>